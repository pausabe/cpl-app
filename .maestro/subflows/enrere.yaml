appId: cpl.cpl
---
# Android: the system back button. iOS has none: the arrow in the header, which the app
# calls «Enrere» for screen readers.
- runFlow:
    when:
      platform: Android
    commands:
      - back
- runFlow:
    when:
      platform: iOS
    commands:
      - tapOn: "Enrere"
